const messages=document.getElementById('messages');const form=document.getElementById('chat-form');const input=document.getElementById('message-input');
function add(text,who='bot'){const el=document.createElement('div');el.className=`bubble ${who}`;el.textContent=text;messages.appendChild(el);messages.scrollTop=messages.scrollHeight}
async function send(text){if(!text.trim())return;add(text,'user');input.value='';try{const r=await fetch('/api/mensagem',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mensagem:text})});const d=await r.json();add(d.resposta||'Não consegui responder.');}catch(e){add('Não foi possível conectar ao servidor.');}}
form.addEventListener('submit',e=>{e.preventDefault();send(input.value)});document.querySelectorAll('[data-message]').forEach(b=>b.addEventListener('click',()=>send(b.dataset.message)));add('Olá! 👋\nEu sou o Assistente Virtual da nossa equipe.\n\nPosso ajudar com:\n1 - Horário de atendimento\n2 - Serviços\n3 - Endereço\n4 - Agendar reunião\n\nDigite uma opção ou escreva sua dúvida.');
